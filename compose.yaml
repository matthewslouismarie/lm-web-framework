services:
  lmwf_dsn:
    build:
      context: ./docker/lmwf
    command: sleep infinity
    volumes:
      - .:/app
    working_dir: /app
  composer_dsn:
    command: sleep infinity
    image: composer:latest
    working_dir: /app
    volumes:
      - ./vendor:/app/vendor