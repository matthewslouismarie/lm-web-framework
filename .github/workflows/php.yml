name: PHPUnit

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Setup
      uses: ./.github/workflows/shared.yml

    - name: Run test suite
      run: vendor/bin/phpunit tests
